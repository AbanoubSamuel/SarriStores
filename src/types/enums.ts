export const maritalStatus = [ "single", "married", "widowed", "divorced", "separated", "other" ];


export enum Roles
{
    ROOT = "root",
    ADMIN = "admin",
    SUBADMIN = "subadmin",
    USER = "user",
}

